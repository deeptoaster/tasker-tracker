export default function EditTrackersStage(props: {
  setTrackers: (trackers: ReadonlyMap<string, ReadonlyArray<string>>) => void;
  trackers: ReadonlyMap<string, ReadonlyArray<string>>;
}): JSX.Element {
  const { setTrackers, trackers } = props;

  return null;
}
